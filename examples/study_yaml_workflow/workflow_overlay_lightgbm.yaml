# 演示配置合并：先加载 BASE_CONFIG_PATH 指向的官方 LightGBM+Alpha158 全流程，
# 再仅覆盖本文件中的字段（此处为 experiment_name）。
# 运行目录：与本文件同目录。命令：python -m qlib.cli.run workflow_overlay_lightgbm.yaml

experiment_name: study_lightgbm_baseline

BASE_CONFIG_PATH: "../benchmarks/LightGBM/workflow_config_lightgbm_Alpha158.yaml"
