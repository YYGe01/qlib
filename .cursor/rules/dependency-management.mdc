---
description: 缺少运行或测试依赖时自动安装并继续验证
alwaysApply: true
---

# 依赖处理规则

- 当执行项目命令、测试、脚本或验证步骤时，如果失败原因是 **Python 包缺失**、**CLI 工具缺失** 或其他明确的环境依赖缺失，默认直接补装所需依赖后继续执行，不必先停下来询问。
- 安装依赖时，优先安装到任务对应的既有环境中。对于本仓库，优先使用 `ai-trader` Conda 环境，而不是 `base` 环境。
- 安装完成后，必须重新运行刚才失败的命令，确认问题是否已被解决；如果仍然失败，要继续定位新的真实阻塞点。
- 如果依赖补装是为了测试或开发流程所必需，应同步更新项目可追踪的依赖声明或规则说明，避免同类问题重复出现。
- 如果失败原因不是缺依赖，而是网络、代理、权限、外部数据、系统配置或上游服务问题，不要误判为缺包；应明确记录真实阻塞原因。
