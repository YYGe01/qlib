---
description: WSL2 下经 Windows 本机代理（如 7897）访问外网的终端用法
alwaysApply: true
---

# WSL2 与 Windows 代理（7897 等）

在 **WSL2** 里跑 `curl` / `wget` / `pip` / `git` 等需要外网时：

1. **`127.0.0.1:7897` 往往不可用**：代理进程在 Windows 上监听，应使用 **Windows 主机在 WSL 中的网关地址**：

   ```bash
   GW=$(ip route show default | awk '{print $3}')
   # 常见形如172.x.x.1，以实际输出为准
   ```

2. **拉 GitHub 等大文件**：若 `http_proxy`/`https_proxy` 指向 `http://${GW}:7897` 出现 **SSL EOF / 中断**，改用 **SOCKS5（远程 DNS）+ HTTP/1.1**：

   ```bash
   curl --proxy "socks5h://${GW}:7897" -fL --http1.1 -o /tmp/out.bin "https://example.com/large.bin"
   ```

3. **需要长期环境变量时**（按需选用协议，与本地代理软件一致）：

   ```bash
   export http_proxy="http://${GW}:7897"
   export https_proxy="http://${GW}:7897"
   # 或：export ALL_PROXY="socks5h://${GW}:7897"
   ```

4. **执行前可快速探测**：`curl -sS -m 5 -o /dev/null -w "%{http_code}\n" --proxy "socks5h://${GW}:7897" https://github.com`

Agent 在为本仓库下载数据、Release、依赖时，应 **优先在终端中设置上述代理再执行**，避免直连超时或失败。
